[package]
name = "barney"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.6.6", features = ["derive"] }
recipes = { path = "../crates/recipes" }
miette = { workspace = true, features = ["fancy"]}
tracing.workspace = true
